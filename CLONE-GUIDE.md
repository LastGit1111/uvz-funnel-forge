# Clone guide

Install dependencies from the lockfile, configure names from `.env.example`, and use fresh preview resources with no production data. Run the build, migrations, automated checks, and smoke test before considering a clone usable.
